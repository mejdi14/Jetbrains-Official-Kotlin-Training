import kotlin.collections.sortBy as sortBy1

fun main(args: Array<String>) {
    var values = mutableListOf(Byte.MAX_VALUE , Short.MAX_VALUE , Int.MAX_VALUE ,Long.MAX_VALUE)
    for (value in values) {
        println(value)
    }
   
}
